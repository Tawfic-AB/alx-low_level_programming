#include "main.h"

/**
 * _abs - Computes the absolute value of an integer.
 * @t: integer to be computed
 * Return: absolute value of t
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
